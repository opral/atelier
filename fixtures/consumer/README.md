# Atelier consumer fixture

This fixture consumes `@opral/atelier` through its package exports, including
the explicit `@opral/atelier/file-icons` and `@opral/atelier/style.css` exports.
It intentionally defines no repository aliases, so its build verifies the
published package boundary.
