# Atelier consumer fixture

This fixture consumes `@opral/atelier` through its package exports, including
the explicit `@opral/atelier/style.css` export. It intentionally defines no
repository aliases, so its build verifies the published package boundary.
