/**
 * The fixed id of the Lix global branch.
 *
 * Lix requires canonical UUID primary keys; the global branch uses this
 * reserved sentinel instead of the historical "global" string id.
 */
export const GLOBAL_BRANCH_ID = "ffffffff-ffff-7fff-bfff-ffffffffffff";
