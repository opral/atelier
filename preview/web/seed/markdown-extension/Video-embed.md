# Video embed

The same `![…](path)` syntax that embeds images embeds video — the extension
picks the renderer, and the alt text becomes the caption.

![Example clip](assets/example-clip.mp4)

Open file on the embed opens the standalone player tab; clicking
`example-clip.mp4` in the Files tree does the same.
